// Given a 32-bit signed integer, reverse digits of an integer.
// Note:
// Assume we are dealing with an environment that could only store integers within the 32-bit signed integer range: [−231, 231 − 1]. For the purpose of this problem, assume that your function returns 0 when the reversed integer overflows.

// Input: x = 120
// Output: 21
// Input: x = -123
// Output: -321

// Input: x = 120
// Output: 21

// Input: x = 0
// Output: 0
function reverseInteger(integer){
    //turn the integer into an array
    //if integer has a negative at array[0], 
    //don't rever that
    //if the integer has a 0, then skip it if the integer
    //only 0 then return that

    // let newArray=[]
    // if(integer===0){
    //     return 0
    // }
    
   //you have to convert the integer into a string 
   //first and then use the .split() method
   //the first check should be whether it is a negative
   //if the array[0] is negative keep it 
   //if it is a negative then keep it at that index, otherwise 

}

reverseInteger(210)